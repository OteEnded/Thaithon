Project handoff for next AI (latest)

Updated at:
2026-03-19 (session 3)

Project name:
Thaithon (เปลี่ยนชื่อจาก Pythai)

Current workspace root:
c:\data\Pythai

Repository:
https://github.com/OteEnded/Thaithon

Old repository (archived):
https://github.com/OteEnded/Pythai
- Archived แล้ว (read-only)
- ไม่ต้องทำอะไรเพิ่มกับ repo นี้อีกแล้ว

Main branch and release status:
- Branch: main
- Latest commits:
	- def0994 [feat]: v0.3.0 - add เว็บ.py, อาร์กิวเมนต์.py, extend ระบบ.py with subprocess
	- a2fb6db [feat]: v0.2.0 - add ระบบ.py, fix เวลา.py, add CI, Python 3.13 support
	- 49325f2 [docs]: mark Pythai repo as archived, project complete
- Tags: v0.1.0, v0.1.1, v0.2.0, v0.3.0 (บน Thaithon repo แล้ว)
- Latest PyPI release: thaithon 0.3.0
- PyPI URL: https://pypi.org/project/thaithon/0.3.0/

Maintainer credit (important):
- Name: OteEnded
- Email: ratnaritjumnong@gmail.com
- Credit/attribution was added to:
	- pyproject.toml (authors)
	- README.md (logo + heading)
	- NOTICE
	- CITATION.cff

License state:
- Current license: Apache-2.0
- Files:
	- LICENSE (Apache 2.0 text)
	- NOTICE (attribution)
	- CITATION.cff (citation metadata)

Packaging state:
- pyproject.toml exists and builds correctly
- Build backend: setuptools
- Package name on PyPI: thaithon (เปลี่ยนจาก pythai ที่ถูกคนอื่น claim ไว้แล้ว)
- py-modules come from libs/:
	- ไพท่อน, ไพท่อน_builtins, คณิต, เวลา, สุ่ม, สถิติ, เจสัน, เรกซ์, เส้นทาง, ซีเอสวี, ระบบ, เว็บ, อาร์กิวเมนต์
- logo.png อยู่ที่ root, ถูก reference ใน README.md ผ่าน GitHub raw URL
- Last validation performed:
	- python -m build => success (0.3.0)
	- python -m twine check dist/* => passed
	- python -m twine upload dist/* => success (0.3.0)
- GitHub Actions CI: .github/workflows/test.yml (runs on Python 3.10-3.13)

Project structure (organized):
- libs/      main modules
- tests/     test scripts
- examples/  usage examples
- tools/     utility/debug scripts
- docs/      handoff notes
- logo.png   project logo (ใช้เป็น PyPI icon ผ่าน README)

Current key files:
- README.md
- pyproject.toml
- LICENSE
- NOTICE
- CITATION.cff
- CHANGELOG.md
- logo.png

Important module coverage already implemented:
- Builtins wrapper: libs/ไพท่อน.py, libs/ไพท่อน_builtins.py
- Math: libs/คณิต.py
- Date/time + sleep: libs/เวลา.py
- Random: libs/สุ่ม.py
- Statistics: libs/สถิติ.py
- JSON: libs/เจสัน.py
- Regex: libs/เรกซ์.py
- File/Path: libs/เส้นทาง.py
- CSV: libs/ซีเอสวี.py
- System/OS: libs/ระบบ.py (os + sys + subprocess)
- Web/HTTP: libs/เว็บ.py (urllib — GET, POST, JSON, download, URL utils)
- Argument parsing: libs/อาร์กิวเมนต์.py (argparse)

Known caveats for next AI:
1) Unicode normalization in Thai identifiers
- Some visually similar Thai words can exist in different Unicode forms.
- Compatibility aliases are intentionally present in some modules.

2) มะรุ่งนี้() ได้รับการแก้ไขแล้ว
- เปลี่ยนชื่อเป็น เวลาตอนนี้() ใน v0.2.0
- มะรุ่งนี้ ยังคงอยู่เป็น backward-compat alias

3) Minor cleanup candidates (not blockers)
- เพิ่ม module ใหม่ได้: sqlite3 wrapper

What has already been done and should not be re-done blindly:
- Do NOT re-initialize git.
- Package name is thaithon (NOT pythai — that name is taken by someone else on PyPI).
- thaithon 0.1.0, 0.1.1, 0.2.0, and 0.3.0 already exist on PyPI — do NOT re-upload same version.
- logo.png already pushed to GitHub and referenced in README.
- ระบบ.py module added in 0.2.0, extended with subprocess in 0.3.0 — do not re-add.
- เว็บ.py and อาร์กิวเมนต์.py added in 0.3.0 — do not re-add.
- GitHub Actions CI workflow exists at .github/workflows/test.yml.

If continuing work in a new chat/session:
1. Workspace root is c:\data\Pythai (folder name ยังไม่ได้ rename แต่เป็น repo Thaithon แล้ว)
2. Run smoke checks:
	 - python examples/y.py
	 - python tests/test_data_libs.py
3. If metadata changes:
	 - update pyproject.toml + README.md + CHANGELOG.md together.
4. Before next release:
	 - bump version in pyproject.toml
	 - update CITATION.cff version
	 - rebuild dist
	 - twine check
	 - twine upload
	 - create git tag: git tag v0.x.x && git push origin v0.x.x

Release command references:
- Build:
	python -m build
- Check:
	python -m twine check dist/*
- Upload TestPyPI:
	python -m twine upload --repository testpypi dist/*
- Upload PyPI:
	python -m twine upload dist/*

Context intent from user:
- Keep the library practical and fun.
- Current function coverage is enough for now.
- Prefer incremental updates over big rewrites.
