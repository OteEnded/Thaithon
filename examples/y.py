from pathlib import Path
import sys

ROOT_DIR = Path(__file__).resolve().parents[1]
LIB_DIR = ROOT_DIR / "libs"
if str(LIB_DIR) not in sys.path:
	sys.path.insert(0, str(LIB_DIR))

from ไพทอน import *

ตัวเลขจำนวนเต็ม = จำนวนเต็ม("12")
ตัวเลขจำนวนจริง = จำนวนจริง("3.5")
รายการตัวอย่าง = [1, 2, 3, 4]

พิมพ์("ผลบวก:", ตัวเลขจำนวนเต็ม + ตัวเลขจำนวนจริง)
พิมพ์("จำนวนสมาชิก:", ความยาว(รายการตัวอย่าง))
พิมพ์("เรียงจากมากไปน้อย:", เรียงลำดับ(รายการตัวอย่าง, reverse=True))
พิมพ์("ค่ามากสุด:", สูงสุด(รายการตัวอย่าง))
พิมพ์("ค่าทุกตัวเป็นบวกไหม:", ทั้งหมดจริง(เลข > 0 for เลข in รายการตัวอย่าง))